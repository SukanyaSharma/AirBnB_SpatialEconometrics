library(splm)
install.packages("splm")
install.packages("plm", dependencies = TRUE)
install.packages("splm", dependencies = TRUE)
install.packages("Ecdat", dependencies = TRUE)
library(splm)
library(spdep)
library(plm)
library(Ecdat)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
rm(data)
detach(data)
detach(data)
detach(data)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
rm(data)
detach(data)
detach(data)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
data.names
data(names)
data(*)
data.names()
fm <- rent_burdened ~ log_MHI + percent_airbnb_all_rentals + percent_bachelors_degree +percent_non_white +percent_foreign_born +percent_unempl)
fm <- rent_burdened ~ log_MHI + percent_airbnb_all_rentals + percent_bachelors_degree +percent_non_white +percent_foreign_born +percent_unempl
library(foreign)
View(data)
View(data)
coplot(rent_burdened ~ Year|Census_Tract, type = 'b')
y <- cbind(rent_burdened)
rm(y)
Y <- cbind(rent_burdened)
X <- cbind(log_MHI, percent_airbnb_all_rentals, percent_bachelors_degree, percent_non_white,
percent_foreign_born, percent_unempl)
rm(fm)
View(data)
rm(data)
detach(data)
detach(data)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
Y <- cbind(rent_burdened)
X <- cbind(log_MHI, percent_airbnb_all_rentals, percent_bachelors_degree, percent_non_white,
percent_foreign_born, percent_unempl)
pdata <- plm.data(data, indexes = c('Id', 't'))
pdata <- pdata.frame(data, indexes = c('Id', 't'))
View(data)
View(data)
View(pdata)
View(pdata)
pdata <- plm.data(data, indexes = c('Id', 't'))
View(pdata)
View(pdata)
summary(Y)
summary(X)
pooling <- plm(Y ~ X, data = pdata, model = 'pooling')
summary(pooling)
X <- cbind(log_MHI, percent_airbnb_all_rentals, percent_bachelors_degree,
percent_foreign_born, percent_unempl)
# Setting data as panel data
pdata <- plm.data(data, indexes = c('Id', 't'))
# descriptive statistics
summary(Y)
summary(X)
pooling <- plm(Y ~ X, data = pdata, model = 'pooling')
summary(pooling)
library(stargazer)
stargazer(pooling)
between <- plm.data(Y ~ X, data = pdata, model = 'between)
between <- plm.data(Y ~ X, data = pdata, model = 'between')
between <- plm.data(Y ~ X, data = pdata, model = 'between')
between <- plm(Y ~ X, data = pdata, model = 'between')
summary(between)
stargazer(between)
between <- plm(Y ~ X, data = pdata, model = 'fd')
between <- plm(Y ~ X, data = pdata, model = 'between')
fd <- plm(Y ~ X, data = pdata, model = 'fd')
summary(fd)
stargazer(fd)
within <- plm(Y ~ X, data = pdata, model = 'within')
summary(within)
stargazer(within)
random <- plm(Y ~ X, data = pdata, model = 'random')
summary(random)
stargazer(random)
summary(random)
plmtest(pooling)
stargazer(plmtest(pooling))
pFtest(fixed, pooling)
pFtest(within, pooling)
phtest(random, within)
library(splm)
library(spdep)
library(plm)
library(Ecdat)
library(stargazer)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
Y <- cbind(rent_overburdened)
X <- cbind(log_MHI, percent_airbnb_all_rentals, percent_bachelors_degree,
percent_foreign_born, percent_unempl)
# Setting data as panel data
pdata <- plm.data(data, indexes = c('Id', 't'))
# descriptive statistics
summary(Y)
summary(X)
# Pooled OLS Estimator
pooling <- plm(Y ~ X, data = pdata, model = 'pooling')
summary(pooling)
stargazer(pooling)
# Between estimator
between <- plm(Y ~ X, data = pdata, model = 'between')
summary(between)
stargazer(between)
# First differences estimator
fd <- plm(Y ~ X, data = pdata, model = 'fd')
summary(fd)
stargazer(fd)
# Fixed Effects or within estimator
within <- plm(Y ~ X, data = pdata, model = 'within')
summary(within)
stargazer(within)
# Random Effects
random <- plm(Y ~ X, data = pdata, model = 'random')
summary(random)
stargazer(random)
# LM test for random effects vs OLS
plmtest(pooling)
# LM Test for fixed effects vs OLS
pFtest(within, pooling)
# Hausman test for fixed vs random effects model
phtest(random, within)
library(splm)
library(spdep)
library(plm)
library(Ecdat)
library(stargazer)
data <- read.csv('Panel_Data.csv')
attach(data)
summary(data)
Y <- cbind(rent_overburdened)
X <- cbind(log_MHI, percent_airbnb_all_rentals, percent_bachelors_degree,
percent_foreign_born, percent_unempl)
# Setting data as panel data
pdata <- plm.data(data, indexes = c('Id', 't'))
# descriptive statistics
summary(Y)
summary(X)
# Pooled OLS Estimator
pooling <- plm(Y ~ X, data = pdata, model = 'pooling')
summary(pooling)
stargazer(pooling)
# Between estimator
between <- plm(Y ~ X, data = pdata, model = 'between')
summary(between)
stargazer(between)
# First differences estimator
fd <- plm(Y ~ X, data = pdata, model = 'fd')
summary(fd)
stargazer(fd)
# Fixed Effects or within estimator
within <- plm(Y ~ X, data = pdata, model = 'within')
summary(within)
stargazer(within)
# Random Effects
random <- plm(Y ~ X, data = pdata, model = 'random')
summary(random)
stargazer(random)
# LM test for random effects vs OLS
plmtest(pooling)
# LM Test for fixed effects vs OLS
pFtest(within, pooling)
# Hausman test for fixed vs random effects model
phtest(random, within)
source('/media/Work/Sukanya/Spatial_Econometrics/Panel Data/Panel_Data_Script_rent_overburdened.R')
source('/media/Work/Sukanya/Spatial_Econometrics/Panel Data/Panel_Data_Script_rent_overburdened.R', echo=TRUE)
source('/media/Work/Sukanya/Spatial_Econometrics/Panel Data/Panel_Data_Script_rent_overburdened.R', echo=TRUE)
source('/media/Work/Sukanya/Spatial_Econometrics/Panel Data/Panel_Data_Script_log_median_rent.R', echo=TRUE)
